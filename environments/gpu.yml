name: Megatron-LM
channels:
  - defaults
  - pytorch
dependencies:
  # python + pip
  - python=3.6
  - pip
  # deps
  - nltk>=3.4
  - numpy>=1.15.4
  - pandas>=0.24.0
  - tensorflow-gpu>=1.12.0,<2.0
  # from https://pytorch.org/
  - pytorch
  - torchvision
  - cudatoolkit=10.0
  # from https://pypi.org/
  - pip:
    - sentencepiece>=0.1.8
